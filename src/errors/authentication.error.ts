export const missing_access_token = `Authentication failed: Access token is missing, make sure that you have logged in before 
  accessing this route.`;

export const invalid_access_token = `Authentication failed: Access token is invalid, make sure that you have logged
succesfully before accesing this route.`;

export const invalidated_session = `Authentication failed: this session has been invalidated`;

export const unauthorized = "Authentication Failed: Login and try again";

export const user_dne = `Authentication failed: This user has already been deleted or doesn't exist`;
